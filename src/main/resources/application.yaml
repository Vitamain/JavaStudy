server:
  port: 8888

# mysql
spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/user?useUnicode=true&characterEncoding=utf-8
    driver-class-name: com.mysql.cj.jdbc.Driver

# mybatis
mybatis:
  configuration:
    map-underscore-to-camel-case: true


# log
logging:
  level:
    com.lkcoffee.dao: trace
